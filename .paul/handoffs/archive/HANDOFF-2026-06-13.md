# PAUL Handoff

**Date:** 2026-06-13
**Status:** paused (mid-discussion: planning run_query pipeline refactor)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Quine — Relation-graph match-rewrite engine (Rust, edition 2024)
**Core value:** e-graph equality saturation with Datalog-style semi-naïve evaluation

---

## Current State

**Version:** v0.5 Refactor & Simplify — 🚧 In Progress
**Phase:** 11 of 3 — Core Engine Simplification ✅ Complete (3/3 plans)
**Next Phase:** 12 — Solver Simplification (not started)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Phase 11 closed — aa3c3b7 committed]
```

**Milestone progress:** v0.5 at 67% (Phase 11 done, Phase 12 + 13 remain)

---

## What Was Done (This Session — 2026-06-13)

**Phase 11 completed (3 plans + transition):**
- 11-01: Extracted CostTracker → `cost.rs` (892→721, −171 lines)
- 11-02: Extracted ReverseIndex → `reverse_index.rs` (721→689, −32 lines)
- 11-03: Eliminated insert/union/fresh_id duplication via `action_ctx()` delegation (689→641, −48 lines)
- Total: `related_egraph.rs` 892 → 641 lines (−251, −28%)
- 46/46 tests pass, committed as `aa3c3b7`

**Post-Phase-11 discussion:**
- User identified deep nesting in `run_query`, `run_semi_naive`, `rebuild` as remaining issues
- Quick-fix plan 11-04 (extract two helper functions: `collect_driver_rule_pairs` + `hash_join_rows`) was **rejected** — "not good enough", just moving nesting around
- User chose **Option A: refactor `run_query` into clear pipeline stages**

---

## What's In Progress

**Planning: `run_query` pipeline refactor** — session paused mid-design.

The agreed direction:
- Decompose `run_query` (~140 lines) into 3 stages:
  1. **Initial scan** (keep inline, simple)
  2. **For each subsequent step:** `scan_step_table()` + `join_step_rows()` + extend vars
  3. **Finalize:** `filter_and_permute()`
- 3 functions to extract: `scan_step_table()`, `join_step_rows()`, `filter_and_permute()`
- Target: `run_query` becomes ~35-line readable pipeline
- This is standard scope (structural refactor, not quick-fix)

The plan was NOT yet written — session paused right after user confirmed "A".

---

## What's Next

**Immediate:** Write PLAN.md for `run_query` pipeline decomposition (standard scope, ~2-3 tasks).

This would be a new plan — could be:
- 11-04 (re-opening Phase 11, since the quick-fix 11-04 was deleted)
- Or a standalone quick plan before Phase 12

**After that:** Phase 12 — Solver Simplification (`relaxation.rs` 637 lines + `solver.rs` 460 lines)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state — v0.5, Phase 11 complete |
| `.paul/ROADMAP.md` | Phase overview — 5 milestones, v0.5 active |
| `.paul/PROJECT.md` | Architecture, 19 decisions, phase history |
| `.paul/phases/11-core-simplification/` | 3 PLANs + 3 SUMMARYs |
| `quine-core/src/related_egraph.rs` | 641 lines — `run_query` at lines 299-439 is the target |
| `quine-core/src/cost.rs` | CostTracker (from 11-01) |
| `quine-core/src/reverse_index.rs` | ReverseIndex (from 11-02) |

---

## Architecture Notes

**Established patterns (Phase 11):**
- `CostTracker` + `ReverseIndex`: struct with private state + methods taking read-only deps
- `action_ctx()`: RelatedEGraph → ActionCtx delegation for mutation (borrow checker exception in `run_semi_naive`)
- 46/46 tests green baseline

**CARL GIT rule:** NEVER include Co-Authored-By in commits.

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume` — will detect Phase 11 complete + this handoff
3. Then: write the `run_query` pipeline PLAN.md and proceed with APPLY/UNIFY
4. Then: Phase 12

---

*Handoff created: 2026-06-13*
