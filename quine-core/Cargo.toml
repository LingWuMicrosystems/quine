[package]
name = "quine-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
std = ["rayon"]
rayon = ["dep:rayon"]

[dependencies]
smallvec = "1.15.1"
rustc-hash = { workspace = true }
rayon = { version = "1.12.0", optional = true }
